# Neon Postgres connection string (from the Vercel > Storage > Neon integration,
# or the Neon dashboard). Used by the /api functions and the seed script.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
